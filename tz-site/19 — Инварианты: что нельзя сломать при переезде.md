---
tags: [ТЗ, приёмка, передача, разработка]
updated: 2026-08-21
---

# 19 — Инварианты: что нельзя сломать при переезде

← [[00 — MOC (карта ТЗ)]] · технические требования — [[04 — Технические требования]]

## Зачем этот файл

Семьдесят четыре проверки живут в `tools/accept.mjs`, и он работает против **прототипа** —
одного HTML-файла. При переезде на боевой стек инструмент перестанет запускаться, а вместе с ним
исчезнут и правила, которых он держал. Здесь они записаны как требования, независимые от стека.

У большинства указана причина. Правило без причины выбрасывают первым, а каждое из этих появилось
после конкретного отказа — своего или увиденного в чужой выдаче.

## Разметка

| Требование | Почему |
|---|---|
| Прямой ответ в `FAQPage` — только текст, без разметки внутри | разметка внутри ответа означает, что развёрнутый блок утёк в то, что читает краулер |
| **Ответ с непроверенным в прямой части в разметку не идёт целиком** | одиннадцать записей отдавали машине «от 2–3 млн ₽ ⚠️ порог уточняется». Вырезать пометку нельзя: тогда неподтверждённое число уходит как утверждённое |
| Число записей `FAQPage` = числу карточек без пометок | иначе разметка расходится с содержимым молча |
| Разметка совпадает с DOM с исполнением JS и без | собранная скриптом разметка не существует для краулеров, не исполняющих JS |
| `Article` на странице ответа, не `QAPage` | `QAPage` — для страниц, где ответы присылают пользователи; у нас один авторский ответ |
| `author` отсутствует, пока имя не подтверждено | заглушка в структурированных данных попадёт в индекс как имя автора |
| В `Organization` только проверяемые поля | непроверенное в разметке цитируется как факт о компании |

## Тексты

| Требование | Почему |
|---|---|
| Заголовок страницы — вопрос в формулировке пользователя | точка входа сопоставляется с запросом. К подзаголовкам внутри ответа не относится |
| Прямой ответ — первые 1–2 предложения | извлекается начало. Требование к **началу**, а не к длине абзаца: ответ из четырёх предложений допустим, если первые одно-два отвечают. Восемь таких проверены — все отвечают сразу |
| **Вопрос «да/нет» получает «да» или «нет» первым словом** | девять из пятнадцати не давали, включая вопрос про взлом, где уход читается особенно плохо. Правило разъезжается при правках: два ответа ушли обратно в обстоятельства и это заметила только проверка — теперь она в приёмке |
| Ни одного дословного повтора предложения между страницами | вес делится, машина выбирает между копиями |
| Сходство любых двух страниц ниже 0.35 по четырёхсловным окнам | пересказ своими словами — тот же отказ, поиском по предложениям не виден |
| **Текст не обещает того, чего на странице нет** | два ответа обещали «демо на этой странице», а модель дома стоит только на главной |
| **Число у кластера совпадает с количеством карточек в нём** | правится руками при добавлении ответа и разъезжается молча: список обещает восемь, внутри девять. Мерить без активного запроса — поиск подменяет это число на количество найденного |
| **Числовое обещание совпадает с длиной списка** | «пять стадий» в прямом ответе против «шести шагов» в блоке под ним. **Считать только настоящие списки.** Перечисление в прозе наивным делением по запятым и союзам даёт ложные срабатывания: пункт сам содержит и запятую, и «и» — «запас жил до каждой группы света и до каждого окна» это один пункт. Четыре «расхождения», найденные так, оказались верным текстом |
| Утверждение о демо совпадает с тем, что демо делает | текст говорил «перекрывает воду», а состояния воды в модели нет вовсе |
| Сравнительное превосходство о себе — только с критерием и источником | ст. 14.2 и 14.3 ФЗ-135, штраф от 100 000 ₽ |

## Адреса и переезд

| Требование | Почему |
|---|---|
| Каждый маршрут есть в `sitemap.xml`, каждый URL карты существует | URL без страницы даёт ошибку сканирования и тратит бюджет обхода |
| **Путь, который отдаёт 410, не закрыт в `robots.txt`** | закрытый от обхода бот не запросит, 410 не увидит, адрес останется в индексе |
| `/articles/` легаси ведётся адресно, без склейки в раздел | там единственная страница, стоящая в органике третьей |
| Зеркало `mimismart.ru` — своими правилами | у доменов расходится транслитерация: `ventilyatsiya` против `ventilyacziya.html` |
| Цель редиректа существует | редирект в никуда хуже отсутствующего |
| Один `title`, `description`, `canonical` на маршрут | дубли делят вес |

## Доступность и поведение

| Требование | Почему |
|---|---|
| Контраст WCAG AA в обеих темах | ночная тема ломалась отдельно от дневной |
| Фокус видим на каждом элементе обхода табом | правиловая проверка этого не покрывает |
| **Ссылка пропуска навигации видна на прокрученной странице** | при `position:absolute` она уезжала за верхний край |
| Нет горизонтальной прокрутки на 360px и при масштабе 200% и 400% | WCAG 1.4.10; прокрутка внутри обёртки таблицы допустима |
| `prefers-reduced-motion` отключает переходы и не оставляет контент полупрозрачным | иначе часть текста просто не видна |
| Таблицы: подпись колонки у каждой ячейки | скринридер иначе читает числа без смысла |
| **Активный сценарий объявляется в разметке, а не только заливкой кнопки** | цвет для скринридера не состояние: пользователь не знает ни какой сценарий идёт, ни что выбор произошёл. `aria-pressed` ровно на одной кнопке, значение совпадает с классом |
| Ряд кнопок сценариев — подписанная группа | иначе восемь кнопок подряд читаются как несвязанные, без объяснения, что это один переключатель |
| **Проходимость проверяется по всему отрезку хода, а не по его концу** | точечная проверка стену не держит: по обе стороны перегородки пол, и один быстрый `pointermove` переносит жителя в соседнюю комнату. Шаг выборки мельче самой тонкой перегородки на плане (измерено: 4.8 px) |
| **При отказе в движении прошлая позиция не обновляется** | иначе запрет прячет след, а человек всё равно телепортируется и следующий шаг делает уже из-за стены. Именно это правило заставляет обводить курсор через проём |
| Маска плана читается один раз в массив; прямоугольники объектов — один раз на событие | трассировка делает десятки проб на движение мыши; поштучный `getImageData` съедает кадр |
| В машине нет шильдиков, эмблем и надписей | узнаваемая марка на иллюстрации — чужой товарный знак в нашем материале |

## Согласие и закон

| Требование | Почему |
|---|---|
| Аналитика не стартует до явного согласия | ст. 9 ФЗ-152 |
| **Переходы по сайту без ответа на баннер согласием не считаются** | позиция РКН: «продолжая пользоваться сайтом» — не согласие |
| Отказ сохраняется и переживает переход | решение, которое переспрашивают до нужного ответа, решением не является |
| Форма не отправляется без отметки согласия | ст. 9 ФЗ-152 |
| Сведения об исполнителе в подвале — на всех страницах | п. 1 ст. 9 ЗоЗПП, до 10 000 ₽ |

## Реестр: какие проверки доказанно умеют падать

Правило «каждый гейт проверять на способность падать» само по себе ничего не гарантирует, пока
не записано, какие проверены. В `tools/accept.mjs` **121 условие отказа**. Проверка идёт
мутацией: в прототип вносится поломка, прогон должен назвать её своими словами.

Прогонять по одной мутации дорого — полный проход идёт минуты. Поэтому мутации вносятся
пачками по 4–10 независимых, и прогон обязан выдать столько же разных сообщений.

**Доказано — все 126 условий:**
разметка (тип `Article`, `dateModified`, расхождение `FAQPage` с карточками, пометка внутри
разметки, разметка без JS) · адреса (маршрут вне `sitemap.xml`, ссылка в `llms.txt` в никуда,
цель редиректа без страницы, `YandexBot` закрыт — в двух формах) · доступность (язык документа,
дубли `id`, картинка без `alt`, SVG без имени, видимый фокус, `aria-pressed` на сценариях в трёх
проявлениях, подпись группы кнопок, подписи колонок в таблицах) · вёрстка (блок шире экрана
телефона, обрезанная подпись кнопки на трёх ширинах, ячейка не разложилась на узком экране) ·
поведение (Esc закрывает форму, назначение полей формы, число и подпись кнопок очистки поиска,
следы под курсором — появление, размер и задержка угасания) · содержание (превосходство без
критерия, непомеченная цена в калькуляторе, кейсы-шаблоны без пометки, единое имя раздела,
сырые теги в тексте, дословный повтор между страницами, расхождение выгрузки с сайтом) ·
согласие (аналитика без согласия и «согласие переходами») · доступность (контраст,
`prefers-reduced-motion` в двух проявлениях, цель нажатия меньше 24 px, видимые крошки) ·
метаданные (`canonical` мимо адреса, пустой `description`, `title` длиннее 60, второй `h1`,
поле `areaServed` в `Organization`) · целостность (ошибка в консоли, падение при запрещённом
хранилище, калькулятор не найден, расхождение `aria-pressed` с классом, страница ответа
с одной входящей ссылкой, маршрут отдаёт 404, один `canonical` на два маршрута) ·
тема (переключатель не меняет тему, системная настройка игнорируется, выбор не переживает
перезагрузку) · таблицы (ячейки без отступов, строки без разделителя) ·
поиск (форма слова не приводится к основе) · тексты (пометка без пояснения, пометка отдельным
абзацем, пропуск уровня заголовка, заголовок не h2 в развёрнутом блоке, не-LI внутри списка,
ссылка на политику ведёт в никуда) · согласие (решение по cookie не восстанавливается) · форма (согласие не обязательно,
отправка без согласия, чекбокса нет, метка не связана, ссылка на несуществующий id) ·
адреса (лишний URL в `sitemap.xml`, служебный путь открыт в `robots.txt`, `llms.txt` ведёт
в никуда) · поиск (совпадение внутри слова, префикс основы) · тексты (обещание демо там,
где демо нет) · документы (заявленное число расходится с фактом) · целостность разметки
(`Organization` не парсится, невалидный JSON-LD) · модель дома (отсутствует) · robots (нет группы `YandexBot`, путь с 410 закрыт от обхода) ·
разметка (`Article` не парсится, несбалансированный тег) · форма (не открывается, не проходит
валидацию с корректным согласием) · поиск (неполный сброс, набор по мере ввода) ·
согласие (аналитика после явного отказа) · содержание (сходство страниц выше порога, дубль
`description` между маршрутами, расхождение разметки без JS, пустая или битая страница,
несогласованная ячейка таблицы, таблица без подписей колонок, заглушка в структурированных
данных, тема при запрещённом хранилище, нет кнопки формы на контактах).

**Что показал разбор реализации.** Половина «непроверяемых» условий поддалась, как только я
перестал подбирать якоря вслепую и прочитал код. Сброс поиска не ломался, потому что при пустом
запросе истину возвращала последняя ветвь условия — совпадение по основам с пустым списком.
Набор по мере ввода держался на двух независимых механизмах сразу, и ломать надо было оба.
Запуск аналитики после отказа висел на одной строке, которую я до того не видел.

Так же поддались и остальные, которые я успел записать в «непроверяемые»: сходство страниц —
подменой карточки целиком, а не одного абзаца; пустая страница — снятием `h1`, а не попыткой
опустошить раздел; тема при запрещённом хранилище — снятием `try/catch` вокруг чтения.

Последними поддались и те два, которые я дважды объявлял недоказуемыми в принципе. Разбор
разметки действительно разносит вложенные `<a>` и `<p>` — но проверка читает не разметку, а
содержимое шаблонов, и туда вложенность можно внести через DOM-интерфейс фрагмента. Внёс — и
проверка промолчала. Причина оказалась в ней самой: она брала `innerHTML` шаблона и разбирала
строку заново, то есть **уничтожала ровно тот дефект, который ищет**. Теперь клонирует узлы;
77 вложенных абзацев и 9 вложенных ссылок называются.

Вывод: слепая мутация даёт ложное «не проверяется». Прежде чем записать условие в
непроверяемые, надо прочитать то, что оно проверяет, и то, как оно это делает. Из одиннадцати,
объявленных устойчивыми, устойчивым не оказалось ни одно.



**Как продолжать.** Брать по пачке из списка недоказанных, вносить мутацию, убеждаться, что
сообщение названо, и переносить строку в доказанные. Мутация, которая не дала отказа, значит
одно из двух: гейт не работает или мутация била мимо — разбирать до ответа, а не списывать.

## Как проверять на новом стеке

Проверки из `accept.mjs` переносимы: они работают с отрендеренной страницей через браузер, а не
с исходником прототипа. Менять придётся способ получения списка маршрутов — сейчас он собирается
обходом ссылок в одном документе, на боевом стеке возьмётся из роутера или `sitemap.xml`.

Три инструмента переносить не нужно, они работают с файлами: `lint-text.mjs` (стандарт языка),
`check-docs-sync.mjs` (числа в документах против прототипа), `check-redirect-coverage.mjs`
(карта против выгрузки из Вебмастера).

**Состояние модели между проверками.** Проверка, которая читает состояние дома, обязана сначала
вернуть страницу в исходное: кликами по сценариям её меняют проверки выше. Без сброса условие
сравнивало не то и срабатывало на исправном тексте.

**Главное правило про сами проверки.** Каждый гейт проверять на способность падать, а не только
на зелёный цвет. За одну сессию семь раз ошибка оказывалась в проверке, а не в сайте: регулярка с
`\b` перед кириллицей не совпадала никогда, клонирование узла ломало учёт скрытых элементов,
подсчёт `<loc>` захватывал пример внутри комментария. Зелёная проверка, которая не умеет падать,
опаснее отсутствующей: она создаёт уверенность.
